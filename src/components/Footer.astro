---
const { url } = Astro.props;
function getClassForURL(url) {
    if (url) return 'hover:underline';
    return 'line-through';
}
---
<div class="grow max-w-6xl py-4">
    <div class="flex max-w-6xl justify-evenly items-center">
        <a class={`px-4 ${getClassForURL(url.prev)} text-center basis-1/6`} href={url.prev}>≪ Previous Page</a>
        <span class="font-semibold text-center basis-4/6">by Morritz</span>
        <a class={`px-4 ${getClassForURL(url.next)} text-center basis-1/6`} href={url.next}>Next Page ≫</a>
    </div>
</div>